<!-- 文章详情页的标签 -->
<% if (post.tags && post.tags.length){ %>
    <i class="fas fa-tags" aria-hidden="true"></i>
    <%- list_tags(post.tags, {
      show_count: false,
      class: 'article-tag'
    }) %>
<% } %>
